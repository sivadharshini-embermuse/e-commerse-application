async function test() { const loginRes = await fetch('http://localhost:8000/api/v1/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'sivadharshini977@gmail.com', password: 'Test@1234' }) }); const loginData = await loginRes.json(); const cookieHeader = loginRes.headers.get('set-cookie'); console.log('Login success:', loginData.success); const ordersRes = await fetch('http://localhost:8000/api/v1/admin/orders', { headers: { cookie: cookieHeader } }); console.log('Orders status:', ordersRes.status); const ordersData = await ordersRes.text(); console.log('Orders data:', ordersData.substring(0, 200)); const usersRes = await fetch('http://localhost:8000/api/v1/admin/users', { headers: { cookie: cookieHeader } }); console.log('Users status:', usersRes.status); const usersData = await usersRes.text(); console.log('Users data:', usersData.substring(0, 200)); } test();